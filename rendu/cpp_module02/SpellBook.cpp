#include"SpellBook.hpp"


// * void learnSpell(ASpell*), that COPIES a spell in the book
// * void forgetSpell(string const &), that deletes a spell from the book, except
//   if it isn't there
// * ASpell* createSpell(string const &), that receives a string corresponding to
//   the name of a spell, creates it, and returns it.
SpellBook::SpellBook(){}
SpellBook::~SpellBook(){
	
}

void SpellBook::learnSpell(ASpell* spell_pt){

}
void SpellBook::forgetSpell(string const & spell_ref){

}
ASpell* SpellBook::createSpell(string const & spell_name){
	ASpell *spell;
	spell =
	return(spell);
}
